Drop your headshot here named exactly: profile.jpg

index.md references it as assets/profile.jpg. Until you add it, the image
on the page will show a broken-image placeholder (everything else works).
A square image ~400x400px looks best (it's displayed as a circle at 140px).
